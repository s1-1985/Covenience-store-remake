# 2026-09-18 Claude Codeセッション引き継ぎ記録(その2)

このファイルは、同日付の`docs/handoff/2026-09-18-claude-code-session-handoff.md`
(PR #203、タスク#31-34)とは**別セッション**の記録。前回セッションの続きとして、
ユーザーから攻略本4分割PDFスキャン(本1/本2/本2-1/本2-2、計115頁)を再度渡され、
「引き継ぎ資料と添付ファイルを読んで状況を理解し、記録内の次にやるべき作業を
始める」よう指示を受けて開始した。

## 1. 最重要の発見: 3.3節の「未解決」記載は古い情報だった

前回の引き継ぎ記録(2026-09-18その1)第3.3節は、以下3項目を「前回セッションから
持ち越し、今回未着手」としていた:

- ベンチの維持費(既存168円 vs 攻略本160円)
- 大型店舗建設費の攻略本内部矛盾(18,000,000円 vs 24,000,000円)
- 顧客の10列行動データの分解(スキャン解像度の限界で2回失敗)

しかし`docs/research/strategy-guide-fixture-crosscheck-2026-09-16.md`第9節
(2026-09-17追記)を読んだところ、この3項目は**このGodotクライアント作業とは別の
セッション(`reference_sim`側、Codexとの共同作業と思われる)によって、2026-09-17
時点で既に解決済み**だったことが判明した:

- ベンチ維持費: ユーザーから同じ4分割PDFを高解像度(400dpi)で再アップロードされ、
  「衝突する内容は攻略本準拠」の明示的指示のもとで160円/日に確定・
  `reference_sim/conveni_sim/baseline_data.py`へ反映済み。
- 大型店舗建設費: 攻略本内部で7箇所が18,000,000円、1箇所のみ24,000,000円という
  「攻略本自身の内部矛盾」と判明。外部資料との対立ではないため、多数決で
  18,000,000円を採用(この判断自体は変更なし)。
- 顧客の10列行動データ: 高解像度再スキャンで全143行を10桁(ス/素/マ/集/買/価/距/
  サ/平/休)明瞭に判読でき、`CUSTOMER_VISIT_SCHEDULE`を完全に置き換え済み。

さらに`game/data/vertical_slice.json`を実際に確認したところ、ベンチの
`maintenance_yen_per_day`は既に`160`(service_bonus`4`)になっており、**Godot
クライアント側にも既にこの解決済みの値が反映済み**だった(タスク#31のstore-grid
移植時、またはfixture_catalog初期作成時に、`reference_sim`の解決済みデータから
直接ポートされていたと推測される。具体的にどのコミットで最初にこの値が入ったかは
未調査)。

大型店舗建設費(18M/24M)については、Godotクライアントがそもそも複数店舗
サイズ(小/中/大)の建設費データを一切実装していない(現状は単一の固定店舗のみ、
店舗拡大は`chain_visitor_milestone.gd`の`try_expand_chain()`が抽象的な土地代
(`LandValuePolicy`)を使うのみで、店舗サイズ別の建設費テーブルは未使用)ため、
この項目は「未解決」ではなく単に「まだ必要とされていない」。

**結論**: 前回の引き継ぎ記録第3.3節の内容は、作成時点で既に古くなっていた
(2026-09-16の引き継ぎ記録の内容をそのまま踏襲し、2026-09-17の`reference_sim`側の
解決を確認していなかったと思われる)。今回この誤りを訂正する。今後、同種の
「攻略本の数値矛盾」系の記録を参照する際は、`docs/research/`配下の最新日付の
crosscheckドキュメントを必ず先に確認すること。

## 2. このセッションでやったこと

1. 添付PDF4点(本1/本2/本2-1/本2-2相当、計115頁)を`pdfimages`で画像抽出し、
   目視で内容を把握(300dpi、コンタクトシート形式で概観 →
   個別ページを高解像度で確認)。
2. 上記の発見(3.3節が既に解決済み)を確認。
3. ユーザーに「3.2節(同時複数顧客/待ち行列、サンプルレイアウト読み込み、什器
   attention差別化)に新規タスク番号を割り振って着手するか」を確認 →
   まず「UIギャップ修正を優先」の回答を得たため、3.2節はいったん対象外のまま
   タスク#35に着手。
4. **タスク#35**(STORE STATUSパネルのUI表示ギャップ解消、前回記録3.1節):
   `vertical_slice_simulation.gd`の`snapshot()`が既に返している
   `star_rating`/`popularity`/`town_population`/`town_store_count_including_
   rivals`/`land_value_yen`を、`main.tscn`のSTORE STATUSパネルに新設した
   ラベルペア(`RatingTitle`/`RatingValue`、`TownTitle`/`TownValue`)で表示する
   よう`main.gd`を配線した。シミュレーション側のロジックは一切変更していない。
   決定書0104。PR #205として作成、CI green確認後ユーザーがready化しマージ済み。
5. ユーザーから「進めて」の指示を受け、3.2節の1件目に着手する判断とした。
   ブランチはPR #205マージ後に`origin/main`から作り直した。
6. **タスク#36**(同時複数顧客・チェックアウト待ち行列、前回記録3.2節1件目):
   `reference_sim/conveni_sim/checkout.py`の`CheckoutStationRuntime`が既に
   複数顧客同時待機・非FIFO強制のサービス選択を実装済みだったことを発見。
   `CustomerRoster`に`can_admit_concurrent()`(新規、容量ベース)と
   `all_settled()`(新規、「入店中の客が0人」)を追加し、既存の単一顧客判定
   `can_admit()`は自動デマンド来店(`demand_admit_if_due()`)専用として温存。
   `start_explicit_customer()`と`start_next_customer()`(「Admit next
   customer」ボタン)の両方が`can_admit_concurrent()`経由で複数客を受け入れる
   ようになり、プレイヤーが通常のプレイ操作でも同時複数客を実際に見られる
   ようにした。`step()`を客ごとの状態遷移と`_dispatch_checkout_queue()`
   (単一レジの直列化、FIFO、REMAKE_BALANCED_DEFAULT)に分離。レイアウト編集の
   安全確認8箇所は`all_settled()`へ移行(`can_admit()`のままだと複数客が
   同時に存在しうる状況で誤動作するため)。決定書0105。PR #206として作成、
   CI green確認後ユーザーがready化しマージ済み。
7. ユーザーから再度「進めて」の指示を受け、3.2節の2件目に着手する判断とした。
   ブランチはPR #206マージ後に`origin/main`から作り直した。
8. **タスク#37**(サンプルレイアウト読み込み機能、前回記録3.2節2件目):
   研究ノート(`docs/research/ss-layout-entrance-register-and-chain-
   cannibalization-2026-09-06.md`第3節)は「サンプルレイアウトが存在し、
   読み込みは簡単には元に戻せない」ことをB+/B級で確認しているが、サンプルの
   具体的な中身・数・正確な取り消し/売却の仕組みは一切未確認。このため
   「メカニズム自体」(読み込み・検証・破壊的上書き)をCONFIRMEDな要件として
   実装し、「サンプルの具体的な中身」(2件: `default_layout`=現状の初期配置と
   同一、`with_bench`=同じ配置+ベンチ1台)はこのプロジェクト独自の
   REMAKE_BALANCED_DEFAULTプレースホルダーとして明示的にタグ付けした。
   `try_load_sample_layout()`は既存のID(什器)を再利用する場合は無料、
   新規追加分のみ通常価格で課金(「正確な売却比率」という別の未確定要素を
   発明せずに済む設計)。取り消し(undo)・売却機能は研究ノートの指針
   (「別々の未解決課題として扱うこと」)に従い実装していない。在庫が
   孤立する組み合わせ(仕入れ済み商品を持つ什器をサンプルが含まない場合)は
   拒否する。`main.tscn`にOptionButton+Buttonを追加し、実際のプレイ操作から
   到達可能にした。決定書0106。PR #207として作成、CI green確認後ユーザーが
   ready化しマージ済み。
9. ユーザーから「俺が前回のセッションでゲームとして動くレベルなのかについて
   聞いたとき、UIのボタンが無いからまだ無理との回答だったけど、現状完成
   までの進捗率はどのくらい?」という質問。調査の結果、タスク#35〜#37の後も
   `vertical_slice_simulation.gd`が実装済みの経済アクション6つ
   (`try_purchase_fixture`/`try_purchase_permit`/`try_procure_product`/
   `try_purchase_promotion`/`try_expand_chain`/`apply_explicit_restock`)が
   `headless_smoke.gd`以外から一切呼ばれていない(UI接続ゼロ)ことが判明。
   これがプレイヤーが実際に「経営」できない最大の原因と回答した上で、
   進捗率について「バックエンドのデータ復元は体感70%程度進んでいるが、
   UIから実際に触れる範囲は体感15〜20%程度」という、測定値ではなく主観的
   見立てであることを明示して回答した。
10. ユーザーから「着手して」の指示を受け、この経済アクションのUI接続に
    着手する判断とした。ブランチはPR #207マージ後に`origin/main`から
    作り直した。
11. **タスク#38**(経済アクションをUIに接続、上記調査で判明したギャップの
    解消): 6アクション全てをSTORE STATUSパネルの新設「Economy actions」
    セクションに接続。什器購入は既存の什器移動(relocate)が使っている
    「タップで配置先を指定する」フローを、`"__new:" + catalog_id`という
    専用プレフィックスで再利用(新しい入力モードを追加していない)。
    什器のinteraction_subcellはカタログに存在しないため、既存データの
    「footprintの外側1マス」という配置慣習から`_find_open_interaction_
    cell()`が自動導出する(2回目のタップを要求しない)。タスク#35〜38の
    追加により固定サイズのパネルに収まらなくなったため、`VBox`を新設の
    `ScrollContainer`配下に移動(`UI/Panel/Margin/Scroll/VBox`)。
    `headless_smoke.gd`に初めて「実際に`main.tscn`をシーンツリーに追加して
    `main.gd`自体をテストする」シナリオを追加した(他の全シナリオは
    `vertical_slice_simulation.gd`を直接操作するのみで、UI層のロジック
    (選択肢の母集団構築、プレフィックスによる配置フロー、interaction_
    subcellの自動導出ヒューリスティック)は今回初めてこの水準のテストを
    必要とした)。決定書0107。
12. PR #208をマージした直後、ユーザーから「初代ザ・コンビニの再現を忘れる
    な、わけのわからないオリジナルゲームに向かうな」という強い指摘。
    `REMAKE_BALANCED_DEFAULT`(独自発明)としてタグ付けした全箇所を棚卸しして
    報告したところ、タスク#38で`main.gd`に追加した2つの独自発明
    (`_find_open_interaction_cell()`の配置ヒューリスティック、補充ボタンの
    数量・コスト既定値)が、決定書0107とPR説明文には「原作の主張ではない」と
    書いていたにもかかわらず、**コード本体には`REMAKE_BALANCED_DEFAULT`の
    タグを付け忘れていた**ことが判明した(`vertical_slice_simulation.gd`や
    `domain/`配下ではタスク#33以降確立している「コード内コメント+JSON
    evidence_note+テストの3箇所に明示する」規律から、UI層(`main.gd`)への
    追加だけが抜け落ちていた)。ユーザーから「続けて」の指示を受け、該当2箇所に
    タグを追加し、`reference_sim`側の契約テストにタグ文字列の存在を検証する
    アサーションを追加して、この種の抜け漏れを機械的に検出できるようにした
    (決定書0107に追記、コードの挙動自体は変更なし)。
13. PR #209マージ後、ユーザーへ「新規発明より配線待ちの確認済みロジックを
    優先する」候補を調査・報告(什器attention差別化は決定0004の対象外指定
    と抵触するため保留のまま)。ユーザーが「商品カタログ拡充」を選択。
    **タスク#39**: `reference_sim`の`PRODUCT_CATEGORY_PRICING`が持つ27の
    CONFIRMED_OFFICIALな商品カテゴリのうち、`vertical_slice.json`の
    `product_catalog`にはタスク#32の「たばこ」1件しか移植されていなかった。
    `try_procure_product`/`main.gd`の`product_catalog_option`は既に
    カタログ非依存の汎用実装だったため、新メカニクスなしで25カテゴリを
    データとして追加(`cash`はATM類似の別メカニクスのため対象外)。
    `initial_stock_units`はたばこの前例(10)を踏襲しつつ、今回は
    `REMAKE_BALANCED_DEFAULT`タグをJSON本体の`evidence_note`に最初から
    明記した(前回の是正で確立した規律をそのまま適用)。決定書0108。
    PR #210として作成、CI(reference-tests)が既存の無関係なflaky test
    (`test_remake_purchase_policy.py`、シードなしRNGによる非決定性、
    ローカル再実行で成功済み)で一度赤くなったが、ユーザーが再実行して
    green化、ready化しマージ済み。
14. ユーザーから「続けて」の指示を受け、次の配線待ち候補に着手する判断と
    した。ブランチはPR #210マージ後に`origin/main`から作り直した。
    **タスク#40**(replenishment_skillの補充所要時間への配線): タスク#33が
    `register_skill`をチェックアウト時間に配線した際、`vertical_slice.json`
    の`skill_evidence_note`は「replenishment_skillも同様にタスク#33が配線
    予定」と書いていたが、実際に配線されたのはregister_skillのみで
    replenishment_skillは未消費のまま残っていた。`_step_restock_tasks()`の
    `_restock_ticks`はどのスタッフが担当しても常に固定値だった。定性的証拠
    (低スキルスタッフは補充作業にも追われて掃除が遅い、agilityが
    replenishmentの上限に関係する)はあるが数式は未確認という、タスク#33の
    register_skillと全く同じ証拠の性質のため、`CheckoutTiming`と全く同じ
    反比例スケーリングの形を再利用する新規`RestockTiming`
    (`game/scripts/domain/restock_timing.gd`)を実装し、`_step_restock_
    tasks()`に配線した。`REFERENCE_REPLENISHMENT_SKILL := 13`は35名
    candidateのreplenishment_skill中央値(register_skillの中央値と偶然
    同じ値)。自動補充タスクは既定で無効のままのため、`headless_smoke.gd`の
    メインシナリオは無変化(734ステップ)。決定書0109。PR #211として作成、
    CI green確認後ユーザーがready化しマージ済み。
15. ユーザーから「什器カタログ拡充(推奨)」を選択(スタッフ雇用・解雇UIは
    新規メカニクス自体が必要なため今回は見送り)。ブランチはPR #211マージ後
    に`origin/main`から作り直した。**タスク#41**(什器カタログを45種類中
    35種類へ拡充): `try_purchase_fixture`/`main.gd`の`fixture_catalog_
    option`は既にカタログ非依存の汎用実装、`store_view.gd`も`kind`が
    `checkout`/`amenity`/`parking`以外なら既定の青色「SHELF」表示に
    フォールバックする(タスク#34で確認済みの既存挙動)ため、コード変更
    ゼロで27種類を`kind: "shelf"`としてデータ追加した。レジ4種(複数レジ
    運用は未実装)・コピー機2種(コピーサービス未実装)・キャッシュ
    ディスペンサー(タスク#39のcash除外と同じATM類似メカニクス)・休憩室
    2種(スタッフ休憩メカニクス未実装)・vending_machine(reference_sim側
    でも主要フィールドが全て未確認のプレースホルダー)の計10種類は、対応
    するメカニクス自体が存在しないため意図的に対象外とした。決定書0110。
    PR #212として作成、CI green確認後ユーザーがready化しマージ済み。
16. PR #212マージ後、ユーザーから改めて強い指摘(意訳)「解読しきれない
    情報でも他の確認済みデータから類推して進めるべきで、勝手な独自要素を
    加えてはならない。直近の作業がそうなっている気がする」。確認したところ
    このリポジトリに`CLAUDE.md`が一切存在せず、この原則自体がどこにも
    明文化されていなかった。タスク#39〜#41を棚卸しした結果、タスク#39で
    追加した`product_catalog`26エントリ全ての`initial_stock_units`を、
    カテゴリごとの規模差を無視して一律「10」にしていたことが判明した
    (実際には同じソース行にCONFIRMED_OFFICIALな`max_capacity`が
    カテゴリごとに存在し、これを無視していた)。タスク#40・#41は再監査した
    結果問題なしと判断(#40は既承認済みの数式形状の再利用、#41は純粋な
    データ移植で数値発明なし)。**タスク#42(是正)**: 全26エントリの
    `initial_stock_units`をカテゴリごとの`max_capacity`から導出する値へ
    変更(たばこも10→40)。この過程で`headless_smoke.gd`の2箇所が
    たばこの旧数値(`10 * 175`等)をハードコードしていたことが判明し、
    設定から動的に読み取る形へ修正。恒久対応として、プロジェクトルートに
    `CLAUDE.md`を新設し、「確認済みデータ→他データからの類推→独自発明
    (要タグ)」という優先順位を明文化した(`PROJECT_MEMORY.md`第15節にも
    追記)。決定書0111。PR #213として作成、CI green確認後ユーザーが
    ready化しマージ済み。
17. ユーザーから「続けて」の指示を受け、配線待ちの確認済みロジックが
    他に残っていないか再調査した。**タスク#43**: `vertical_slice.json`の
    `staff.skill_evidence_note`が「service_skill/security_skill/
    cleaning_skillは未消費」と主張していたが、これはタスク#27時点で
    既に誤りになっていた(`_evaluate_store_rating()`が毎月3スキル値を
    `store_value.gd`の3関数へ渡し、月次スターレーティングに反映済み)。
    `demand_policy.gd`の冒頭コメントにも同様の古い前提が残っていたため、
    合わせて是正した(ロジック・テストのアサーション変更なし、純粋な
    ドキュメント是正)。決定書0112。同じ調査で、優先順位判断が必要な
    候補2件も見つかった(什器の`capacity`/`compatible_product_categories`
    配線は過去3回意図的に未消費のまま据え置かれている経緯があり単純な
    見落としとは言い切れない、`remake_customer_share.py`の0-100
    シェア式を`demand_policy.gd`へ統合するのは中規模の統合作業)。
    ユーザーへの確認待ち。PR #214として作成、CI(reference-tests)が
    セッション中3回目の既知の無関係なflaky testで一度赤くなったが、
    ユーザーが再実行してgreen化、ready化しマージ済み。
18. ユーザーが「顧客シェア式の統合を進める」を選択。**タスク#44**:
    `reference_sim`の`remake_customer_share.py`が持つ`compute_customer_
    share_percent()`(popularity/service/cleaning/security/品揃え/営業時間
    の6要素加重平均、既にREMAKE_BALANCED_DEFAULTタグ済み)を、新規
    `game/scripts/domain/customer_share.gd`として重み・定数を完全一致で
    移植し、`_evaluate_store_rating()`(タスク#27で毎月既に計算済みの
    service_value/security_value/cleaning_valueがある箇所)へ配線した。
    調査の過程で`popularity`(人気度)自体がsnapshot()とセーブ/ロード
    以外どこにも使われておらず、客数シミュレーションに一切影響して
    いなかったことも判明し、これも合わせて配線した。天候・ライバル希釈は
    `demand_policy.gd`側で既に来店客数全体に適用済みのため、二重計上を
    避けて意図的に移植しなかった(ファイル冒頭コメントに明記)。決定書
    0113。PR #215として作成、CI green確認後ユーザーがready化しマージ済み。
19. ユーザーから「システム面を先に全て作って、最後に画像生成、音声生成、
    そしてシステムと画像・音声の紐付けを行う。システムの作り込みを
    進めて」という開発順序の方針が示された(今回初めて明示、既存の作業
    方針とも合致)。以後のセッションもこの順序(システム→アセット生成→
    紐付け)を踏襲すること。この方針に基づき、ユーザーが「什器の
    capacity/互換性を実際に配線」を選択。**タスク#45**: `fixture_catalog`
    の29 shelf種エントリ全てにCONFIRMED_OFFICIALな`capacity`/
    `compatible_product_categories`を追加し(`product_catalog`の
    catalog_idと同じ語彙)、`try_procure_product()`に互換性チェックと
    容量キャップを配線した。什器カタログ制度導入前からのプロトタイプ
    什器(shelf-1/shelf-2)は確認済みデータがないため対象外のまま。
    容量キャップは「商品カテゴリの一般的なmax_capacity(タスク#42)」と
    「実際に購入した特定の什器のcapacity」という2つの確認済みだが
    矛盾しうる値を、小さい方を採用することで解消した(新しい数値の発明
    ではない)。既存のたばこ仕入れテスト・経済UIのE2Eテストが新ルールで
    壊れたため動的化・修正し、互換性の拒否/受理を検証する新規シナリオを
    追加。決定書0114。PR #216として作成、CI green確認後ユーザーがready化
    しマージ済み。
20. ユーザーが「什器のmaintenance_yen_per_day→日次収支」を選択。
    **タスク#46**: `fixture_catalog`全35エントリが持つCONFIRMED_OFFICIALな
    `maintenance_yen_per_day`が一切消費されていなかった。新規
    `_apply_daily_fixture_maintenance()`を`_handle_day_boundary()`から
    呼び出し、所有する全什器(カタログ由来のもののみ)の維持費を合計して
    毎シミュレーション日1回控除するようにした。既存の月次×8投影は現金差分
    をそのまま読むだけの仕組みのため、追加のスケーリング処理は不要だった。
    調査の過程で判明した`salary_yen_per_day_24h`(スタッフ給与、35名候補
    データにのみ存在し実働staff.membersには無い)は今回の対象外とし、
    将来タスクの候補として記録。全35エントリのevidence_noteから「未消費」
    の古い記述を除去。決定書0115。PR #217として作成、CI green確認後
    ユーザーがready化しマージ済み。
21. タスク#46で見つけた「スタッフ給与も未消費」という候補にユーザーの
    明示的な選択を待たず(直前の調査で既にデータを確認済みだったため)
    続けて着手。**タスク#47**: `salary_yen_per_day_24h`をタスク#32と同じ
    「候補者データを`staff.members`へ複製する」パターンで複製し
    (staff-1=7680円、staff-2=7920円)、新規`_apply_daily_staff_wages()`
    を`_apply_daily_fixture_maintenance()`と全く同じ形で追加した。実労働
    時間による按分はスタッフの勤務時間追跡機構自体が存在しないため対象外
    とし、確定額をそのまま日額として控除。この変更により月末決済テスト・
    広告発火テストの期待値計算が壊れたため、日境界を跨いだ回数を動的に
    追跡する形に修正(特に月末決済テストの`expected_cash_after_month_end`
    は、日境界での支出が存在しなかった旧世界でたまたま成立していた式を、
    `_settle_month_end()`の実装から直接導ける頑健な式に置き換えた)。
    `staff_candidates_evidence_note`も、タスク#43是正と同種の陳腐化
    (register_skill/replenishment_skillを「将来消費予定」のまま古い記述に
    していた)が見つかったため併せて是正。決定書0116。

## 3. 検証

- `headless_smoke.gd`: タスク#35は構造チェックに新ラベルのノードパスを追加、
  タスク#36は同時複数顧客・待ち行列を検証する専用シナリオ2件を追加。Godot 4.3
  公式バイナリでPASS(タスク#35時点530ステップ→タスク#36で614ステップ)。
- Xvfb + 実Godotバイナリで`main.tscn`を実際にレンダリングし、タスク#35は
  「☆☆☆☆☆ (popularity 0)」「population 2,000, 0 rival stores, land
  ¥24,400,000」、タスク#36は「Admit next customer」を追加操作して
  「2 active — customer-1: to_shelf, customer-2: to_shelf」が意図通り
  表示されることをそれぞれスクリーンショットで目視確認。
- `reference_sim/tests`フルスイート: タスク#35時点651件+xfail1件PASS
  (無影響確認)。タスク#36では既存テスト1件を改名・訂正(「同時来店を
  一切発明していない」という古い主張の訂正)、新規テスト1件を追加、
  スキーマバージョン依存/インデント依存の既存アサーションを2箇所更新し、
  652件+xfail1件PASS。タスク#37では新規テスト1件を追加、スキーマバージョンの
  期待値を更新し、653件+xfail1件PASS。
- タスク#37はheadless_smoke.gdに専用シナリオ(無料再利用/資金不足拒否/
  新規什器の課金/再読み込みで二重課金しないこと/未知ID拒否/入店中ロック/
  在庫孤立の拒否)を追加、704ステップでPASS。Xvfbで客の会計完了後に
  `with_bench`を読み込み、ベンチが実際に描画され現金が2000円減額される
  ことを目視確認。`reference_sim/tests`は新規テスト1件を追加し653件+xfail1件
  PASS。
- タスク#38はheadless_smoke.gdに`main.tscn`をシーンツリーへ実際に追加する
  新規シナリオを追加(什器購入→許可証購入→什器購入→商品仕入れ→補充→
  広告購入→チェーン拡大のエンドツーエンド)、734ステップでPASS。Xvfbで
  スクロール前後2枚のスクリーンショットを撮影し、「Economy actions」
  セクションの什器カタログ選択肢(価格表示含む)が正しく表示されること、
  スクロールが機能することを目視確認。`reference_sim/tests`は新規テスト1件
  を追加し654件+xfail1件PASS。

## 4. Godot実行環境について

前回記録の通り`/tmp`配下はコンテナと共に消えるため、このセッション開始時点では
Godotバイナリが存在せず、`https://github.com/godotengine/godot/releases/
download/4.3-stable/Godot_v4.3-stable_linux.x86_64.zip`から再ダウンロードした
(`/tmp/claude-0/godot-test/`)。次回セッションでも同様に消えている前提で対応
すること。

## 5. 現時点で残っているタスク(次にやるべきこと)

1. **3.2節の残り1項目**(什器attention差別化)は今回も対象外のまま。
   `docs/decisions/0004`の「deliberately absent: incidental/add-on purchase
   probability」という明示的な据え置きと衝突する(attentionが影響しうるのは
   「ついで買い」の発生確率であり、この確率自体がまだ一切実装されていない)。
   着手する場合はまずこの境界を外すかどうかをユーザーに確認すること
   (タスク#33のregister_skillと同様、「衝突する場合は3箇所でタグ付けすれば
   実装してよい」という前例はあるが、今回はまだその確認を取っていない)。
   これで3.2節の3項目のうち2件(同時複数顧客・待ち行列/サンプルレイアウト
   読み込み)が完了、残り1件のみ。
2. `PROJECT_MEMORY.md`第19節末尾の既存ロードマップ(観測リプレイとの接続等)は
   今回変更なし、引き続き有効。
3. 什器の`maintenance_yen_per_day`(ベンチ含む全什器)は、依然として
   このクライアントのいかなるロジックにも消費されていない(evidence_noteに
   明記済み)。日次キャッシュフローへの反映は別タスクとして残っている。
4. 大型店舗建設費データ(18,000,000円、小/中/大の3ティア)は、複数店舗サイズを
   選べる新規出店UIが実装される時になって初めて必要になる。現時点では
   `game/`側に実装先が存在しないため、着手不要。
5. タスク#36で実装した待ち行列は「客はチェックアウトの対話セルに留まり、
   描画側で視認性のためオフセットするのみ」で、専用の列セル座標・レジの
   向き依存の列方向(研究ノート第2節)は未反映。座標が確定した場合は
   `waiting_checkout`フェーズの客の描画位置を専用の列セルへ差し替える余地が
   ある。
6. タスク#37の`sample_layouts`は現状2件(`default_layout`/`with_bench`)のみ。
   店舗サイズ別のサンプル対応関係、正確な取り消し/売却の仕組みは依然として
   未確認のまま(研究ノートが明示的に「別課題」としている)。追加のサンプルを
   増やす場合も、内容自体はREMAKE_BALANCED_DEFAULTのプレースホルダーとして
   扱い、原作データだと主張しないこと。
7. タスク#38で経済アクションはUIから到達可能になったが、「ゲームとして
   動くレベル」にはまだ以下のギャップが残っている(次回セッションで
   「進捗率」を再び聞かれた場合の参考):
   - 什器カタログはタスク#41で45種類中35種類まで拡充済み(決定書0110)。
     未移植の残り10種類(レジ4種・コピー機2種・キャッシュディスペンサー・
     休憩室2種・vending_machine)は、対応するメカニクス自体が未実装の
     ため意図的に対象外。
   - 商品カタログはタスク#39で27種類中25種類(`cash`を除く)まで拡充済み
     (決定書0108)。残る商品側のギャップは、どの棚にも任意の商品を置ける
     (`compatible_fixtures_text`が未消費)ことと、季節需要
     (`seasonal_demand`)が未反映であること。
   - 店舗は固定サイズの小型店1つのみ実際にシミュレートされている。
     `try_expand_chain()`は`player_store_count`という数字を増やすだけで、
     2店舗目は実在しない(タスク#30の既存の割り切り、変更なし)。店舗
     サイズ(小/中/大)の選択UIも存在しない。
   - スタッフの雇用・解雇UIは存在しない(staff-1/staff-2の2名固定)。
   - 補充(restock)の数量・発注ロジックはUIの単純な既定値
     (`initial_stock_units`分をその都度補充)であり、原作の正確な発注
     フローの再現ではない。
   これらはいずれも「進捗率」を評価する際に考慮すべき残存ギャップである。
8. **重要**: ユーザーから「初代ザ・コンビニの再現を忘れるな」という指摘を
   受けた経緯(2.項目12参照)を踏まえ、今後`game/scripts/`(特に`main.gd`の
   ようなUI層)に独自発明(REMAKE_BALANCED_DEFAULT)を追加する際は、
   `vertical_slice_simulation.gd`/`domain/`配下で確立済みの規律——
   コード内コメント+JSON evidence_note+テストの3箇所全てに明示する——を
   最初から徹底すること。決定書やPR説明文だけに書いて満足せず、コード
   本体に`REMAKE_BALANCED_DEFAULT`という文字列を必ず残すこと。可能であれば
   `reference_sim`側の契約テストにそのタグ文字列の存在を検証するアサーションも
   追加し、機械的に検出できるようにすること(今回のタスク#38是正で確立した
   パターン)。
9. **重要**: プロジェクトルートに`CLAUDE.md`を新設した(2.項目16参照)。
   今後のセッションは起動時にこれを読むこと。核心は「確認済みでない値を
   埋める必要がある場合、(1)確認済み証拠 → (2)他の確認済み関連データからの
   類推 → (3)このプロジェクト独自の発明、の順で優先し、(3)に飛ぶ前に
   同じソース行・同じ対象の他の確認済みフィールドに使える数値がないか
   必ず確認すること」。タスク#39→#42の`initial_stock_units`(一律10→
   カテゴリごとのCONFIRMED `max_capacity`)が悪い例・良い例の具体的な
   前例になっている。

## 6. このセッションのPR一覧

| PR | 内容 | 結果 |
|---|---|---|
| #205 | タスク#35(STORE STATUSパネルのUI表示ギャップ解消) | マージ済み |
| #206 | タスク#36(同時複数顧客・チェックアウト待ち行列) | マージ済み |
| #207 | タスク#37(サンプルレイアウト読み込み機能) | マージ済み |
| #208 | タスク#38(経済アクションをUIに接続) | マージ済み |
| #209 | タスク#38是正(REMAKE_BALANCED_DEFAULTタグ付け漏れの修正) | マージ済み |
| #210 | タスク#39(商品カタログを27種類中25種類へ拡充) | マージ済み |
| #211 | タスク#40(replenishment_skillの補充所要時間への配線) | マージ済み |
| #212 | タスク#41(什器カタログを45種類中35種類へ拡充) | マージ済み |
| #213 | タスク#42是正(initial_stock_unitsを類推ベースの値へ是正、CLAUDE.md新設) | マージ済み |
| #214 | タスク#43(service/security/cleaning_skill「未消費」記述の是正) | マージ済み |
| #215 | タスク#44(customer_share_percentを月次で再計算するよう配線) | マージ済み |
| #216 | タスク#45(什器のcapacity/compatible_product_categoriesを仕入れ処理へ配線) | マージ済み |
| #217 | タスク#46(什器のmaintenance_yen_per_dayを日次収支へ配線) | マージ済み |
| (作成予定) | タスク#47(スタッフのsalary_yen_per_day_24hを日次収支へ配線) | 作業中 |

## 7. 作業ブランチについて

`claude/handover-docs-next-tasks-gapdxu`が指定ブランチ。
