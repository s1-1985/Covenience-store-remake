# 攻略本フルデコード vs 既存データ クロスチェック（2026-09-16、2026-09-16 原本精読で追記）

対象コミット: `s1-1985/covenience-store-remake` `claude/code-review-6s2a9n` ブランチ。

本メモは当初 `docs/research/strategy-guide-full-decode-2026-09-16.md`（Codex が攻略本スキャン
から抽出した実装可能データ）を `reference_sim/conveni_sim/baseline_data.py` へ取り込む際に
発見した数値不一致・対応不明点を記録したもの。攻略本ガイド自身の方針（該当ファイル
section 0/17/44）に従い、**既存の確認済み値は上書きしない**。差分はここに残し、判断が必要な
項目として扱う。

**追記（同日）**: ユーザーからPDF原本4分割スキャン（本1/本2系の実物ページ、計115頁）を
直接渡され、Claude Codeが全頁を精読した。full-decode文書がデータ表ページに偏っていたのに
対し、原本には数値公式（サービス値・セキュリティ値・清掃値・時給・地価の計算式）、イベント
発生条件表、店舗評価の増減式、店舗建物の精密寸法など、実装に使える情報がChapter 1〜4全体に
散らばっていた。以下、更新・追加した内容を記す。

## 1. ベンチのメンテナンス費（数値矛盾・未解決）

- 既存値: `168` 円/日 (`WIKI` 出典、`EvidenceLevel.CONFIRMED_COMMUNITY`)
- 攻略本値: `160` 円/日（`strategy-guide-full-decode-2026-09-16.md` 4.5 節、家具データ表）
- 対応: `baseline_data.py` の `bench` エントリでは既存の `168` を維持し、攻略本値では上書き
  していない。両出典とも購入価格・footprint・attention 等の他フィールドとは矛盾していない。
- 要判断: どちらが実機の正しい値か未検証。日本語版/PS版とSS版の版差、または攻略本の誤植・
  Wiki側の伝聞誤りの可能性がある。動画等の一次証拠での再確認が必要。

## 2. 宣伝（プロモーション）の人気上昇値（★解決済み・2026-09-16原本精読で確定）

`PROMOTIONS` の `airship` / `radio` / `tv` について、費用・発火日・発火時刻は攻略本と既存値が
完全一致するが、`popularity_gain` のみ食い違う。

| id | 費用 (既存/攻略本、両者一致) | 発火日/時刻 (両者一致) | 既存 popularity_gain | 攻略本 popularity_gain |
|---|---|---|---|---|
| airship | 1,000,000 | 毎月3日 15:00 | +30 | +40 |
| radio | 3,000,000 | 毎月1日 17:00 | +50 | +60 |
| tv | 5,000,000 | 毎月1日 19:00 | +100 | +90 |

- 既存値の出典: `docs/research/promotion-cost-popularity-and-timing-2026-09-06.md`
  （初代Wiki準拠、evidence level "B+ / FIRST-TITLE-DEDICATED-COMMUNITY-DATA"）。
- **解決**: 攻略本原本の「広告データ」ページ（本1、書籍頁120-121）を直接確認したところ、
  ダイレクトメール・新聞広告の費用/発火日時/効果は既存値と完全一致し、飛行船/ラジオCM/
  テレビCMの効果だけが `+40/+60/+90` と明記されていた（既存の `+30/+50/+100` ではない）。
  同じ数値は本1の別ページ（オールテクニックガイド、書籍頁77「顧客を呼び込む効率的な宣伝」）
  にも独立して掲載されており、原本内で二重に確認できた（full-decode文書の記載と合わせると
  四重確認）。よって `baseline_data.py` の `PROMOTIONS` を更新し、`airship`/`radio`/`tv` の
  `popularity_gain` を攻略本値（`EvidenceLevel.CONFIRMED_OFFICIAL`）に置き換えた。`cost_yen`/
  `trigger_day`/`trigger_hour` は変更していない（両出典で完全一致していたため）。

## 3. 店舗建物サイズの対応関係（★大幅に解決・一部は攻略本内部矛盾として残る）

- 既存 `STORE_VARIANTS` の `small_top`: `construction_price_yen=6,000,000`,
  `editable_floor=(8, 13)`（`PS small-store visual reconstruction` 出典）。
- full-decode文書の `store_1` 表記（店内7x10/店舗外形9x10/総面積100等）は列の対応関係が
  不明だったが、原本の「店舗データ」ページ（本1、書籍頁106-109）を直接確認したところ、
  実際には**6つの店舗バリエーション**（小/中/大 × 縦/横の転置）が別々の行として掲載されており、
  列は「建物代／店舗内(縦×横)／店舗外周(縦×横)／屋外スペース(縦×横)／総面積／建物全体／
  床面積／店外スペース」であることが判明した:

  | 店舗 | 建物代 | 店舗内 | 店舗外周 | 屋外スペース | 総面積 | 建物全体 | 床面積 | 店外スペース |
  |---|---|---|---|---|---|---|---|---|
  | 店舗1 | 6,000,000 | 5×8 | 7×10 | 3×10 | 100 | 70 | 40 | 30 |
  | 店舗2 | 6,000,000 | 8×5 | 10×7 | 10×3 | 100 | 70 | 40 | 30 |
  | 店舗3 | 12,000,000 | 7×10 | 9×12 | 3×12 | 144 | 108 | 70 | 36 |
  | 店舗4 | 12,000,000 | 10×7 | 12×9 | 12×3 | 144 | 108 | 70 | 36 |
  | 店舗5 | 18,000,000 | 8×12 | 10×14 | 3×14 | 196 | 154 | 108 | 42 |
  | 店舗6 | 18,000,000 | 12×8 | 14×10 | 14×3 | 196 | 154 | 108 | 42 |

  すなわち full-decode 文書の「店内」列は実際には「店舗外周」の値であり、真の「店舗内
  (可食フロア)」列は別に存在していた（脱落/誤記）。
- 既存の `small_top.editable_floor=(8, 13)` (=104マス) は、店舗1/2の「店舗内」5×8=40マスとも
  「店舗外周」7×10=70マスとも一致しない。数値の出所（`PS small-store visual reconstruction`）
  が弱い証拠レベルであることに加え、この原本データ表との不一致がより明確になったため、
  **要判断として残す**（今回は上書きしない）。`large_top.editable_floor=(13, 14)`（WIKI由来）
  も店舗5/6の店舗内8×12/12×8とは一致せず、同様に未解決。
- 建設費（`construction_price_yen`）は縦横の転置に関わらず小=6,000,000/中=12,000,000/
  大=18,000,000で一貫しているため、こちらは安全に追加した（`small_bottom`/`medium_top`/
  `medium_bottom`/`large_top`/`large_bottom`の空欄フィールドを埋めるのみで、`editable_floor`
  等の既存値には触れていない）。
- **新規に発見した攻略本内部矛盾**: 大サイズについて、原本の別ページ（本1、書籍頁10-11、
  店舗選択画面のスクリーンショット）では「店舗規模・大 店舗サイズ縦(横)16×16 建設費用
  24,000,000円」と明記されている。これは上記データ表の18,000,000円、および本1のレイアウト
  実例集6箇所（書籍頁78-84、ケースNo.3/5/6/7/8で「大 サイズ16×16 価格1800万」と表記）の
  いずれとも一致しない。7箇所が18,000,000円、1箇所のみ24,000,000円という多数決になるため、
  `baseline_data.py` では18,000,000円を採用したが、**攻略本自身の内部矛盾**として記録する
  （二次資料同士の対立ではなく、同一書籍内での数値相違）。
- 要判断: `editable_floor` の正しい値（店舗内寸法 vs 現行の`(8,13)`/`(13,14)`）、および
  「16×16」というサイズ表記と「店舗外周」寸法の対応関係は、Codex側または実機での再確認が
  望ましい。

## 4. サービス設備の service_bonus（数値矛盾・新規発見・未解決）

原本の「オールテクニックガイド・内装関連」ページ（本1、書籍頁69）と「設備データ表の備考欄」
（本1、書籍頁118-119）の両方で、以下が独立に二重確認された:

| 設備 | 既存 `service_bonus`（WIKI由来） | 攻略本値 |
|---|---|---|
| 観葉植物 (potted_plant) | +2 | +2（一致） |
| ベンチ (bench) | +3 | +4（不一致） |
| 噴水 (fountain) | +25 | +30（不一致） |

`potted_plant` は一致したため対応不要。`bench`/`fountain` は項目1のメンテナンス費矛盾と
同様のパターン（既存値と攻略本値が近いが異なる）のため、**既存値を上書きせず**そのまま
維持した。要判断として記録する。

## 今回クロスチェックで独立確認できた一致（参考）

以下は既存の確認済み値と攻略本値が完全一致し、出典の信頼性を相互に裏付けたもの。
`baseline_data.py` では、これらの一致を根拠に、従来 `None` だった未確認フィールド
（`purchase_price_yen` / `placement` / 一部 `footprint`）のみを追加した。

- `copier_a`（小型コピー機）: capacity 20 / attention 10 / purchase_price 1,500 /
  maintenance 1,200 で一致。
- `copier_b`（中型コピー機）: capacity 40 / attention 15 / purchase_price 2,000 /
  maintenance 1,440 で一致。
- `potted_plant`: footprint (1,1) / maintenance 120 で一致。
- `fountain` / `parking_ground` / `parking_two_story` / `parking_tower`: footprint・
  maintenance・parking_capacity で一致。

## 5. 社員の基本給フィールドの単位訂正（★訂正済み）

`SalaryTableEntry.monthly_salary_yen`（models.py）というフィールド名は誤りだった。原本の
該当ページ（本1、書籍頁27）には明確に「◆年齢別・社員の基本時給(円)」と印字されており、
「月給」ではなく「時給」である。別ページ（オールテクニックガイド、書籍頁71）には
「社員の基本時給額＝年齢×10+100(円)」という生成式も掲載されており、既存の10点
（年齢15〜60、5歳刻み）すべてを誤差なく再現することを確認した。人件費の計算式
「人件費＝時給額×営業時間」という記述（クイックリファレンス、書籍頁9付近）とも整合する。
→ `SalaryTableEntry.monthly_salary_yen` を `hourly_wage_yen` にリネームした（数値・
`EvidenceValue`自体は変更なし、ラベルのみ訂正）。

## 6. 誘致施設リストの拡張（★新規追加・多重確認済み）

原本の「誘致できる施設リスト」（本1、書籍頁44-45）と「学校・セキュリティ以外の誘致物件」表
（本2系クイックリファレンス、書籍頁10-11）を照合したところ、既存 `TOWN_FACILITIES` の6件
（`police_box`/`company`/`pool`/`vocational_school`/`university`、および`station`は別出典）
の援助額はすべて完全一致した（6/6）。この高い整合性を根拠に、以下を新規追加した:

- 新規12施設: `mansion`/`gym`/`athletic_field`/`event_hall`/`kindergarten`/
  `elementary_school`/`middle_school`/`high_school`/`park`/`aquarium`/`zoo`/
  `amusement_park`（サイズ・援助額つき、一部は客数=`shopping_population`も判明）。
- 既存施設への追記（従来 `None` だったフィールドのみ）: `police_box`/`company`/`pool`/
  `vocational_school`/`university` の `footprint`、`company`/`pool` の `shopping_population`、
  `fire_station` の `inducement_aid_yen`（600,000円）・`shopping_population`（12人）・
  `footprint`（2×3）。
- `TownFacilityAnchor` に `footprint: Optional[EvidenceValue]` フィールドを新規追加
  （`FixtureDefinition.footprint` と同じパターン）。

## 7. 未実装のまま記録する新規公式（今回はデータのみ、ランタイムへの組み込みは見送り）

以下は原本の「完全攻略オールテクニックガイド」節（本1、書籍頁68-79）に明記されていた計算式。
`master_audit.py`／`store_runtime.py`／`checkout.py` 等への組み込みは、既存ロジックとの整合性
検証やテスト設計が必要な別作業と判断し、**今回はデータの transcription のみ**に留めた。
Codex側での実装検討、またはClaude Codeへの別途の明示的依頼を推奨する。

- **サービス値**: `店舗のサービス値 = 社員3人のサービス値平均 + サービス設備の付加効果`
  （観葉植物+2、ベンチ+4、噴水+30。ベンチ/噴水は上記項目4の通り既存値と矛盾あり）。
- **セキュリティ値**: `店舗のセキュリティ値 = 社員のセキュリティ値合計 × 店舗規模別基準値 + セキュリティ施設の効果`。
  セキュリティ施設の効果: 交番=店舗周囲16×16エリア内、1エリアごとに+10（最大+40）、
  消防署=同+5（最大+30）。店舗規模別基準値: 10×10の店舗=1.5、12×12の店舗=1.65、
  14×14の店舗=1.8（この「14×14」表記は項目3の店舗サイズ表記ゆれの一つ）。
- **清掃値**: `店舗の清掃値 = 社員の清掃値合計 × 店舗規模別基準値`（同上の基準値）。
- **新規出店時の土地代**: `土地代 = 地価(4エリア分) + 建物評価額/2`（複数施設を撤去する
  場合は合計額の1/2）。
- **宣伝効果**: `宣伝の効果 = 店舗の現在のランク評価値 + 宣伝効果`。ランク評価が3つ星以下の
  店舗は、宣伝効果が1日ごとに減衰する。
- **店舗評価（★1〜5）の増減式**: 内部評価値0〜100を星に変換
  （100=★5, 80-99=★4, 60-79=★3, 40-59=★2, 20-39=★1, 0-19=★0）。毎月1日判定。
  価格/サービス/セキュリティ/清掃/売上の5項目中3つ以上をクリアで+5、下記条件を1つ満たす
  ごとに-1（本1書籍頁38-39と書籍頁74-75の2箇所でほぼ一致する表を確認、数値細部は要再照合）。
  → 2026-09-25再照合で2箇所は全セル完全一致と判明(☆0行も両方に印刷あり)。以前の差異は転記ミス。`store-rating-table-reverification-2026-09-25.md`参照。
  その他: お客に怒られる(1/6の確率)で-1、万引きで-1、寄付イベントで+5。
- **イベント発生条件**（本2系クイックリファレンス、書籍頁12）:
  | イベント | 発生条件 |
  |---|---|
  | 寄付 | 手持ち資金15億円以上で月が変わる（10億円以上を強制的に寄付、店舗評価が上がる） |
  | 万引き | 顧客のマナー値 > 店舗の警備値 |
  | 火災/強盗 | 店舗周囲16×16エリアにセキュリティ施設（交番/消防署）が無い場合（警備値より人気が高いと発生しやすい） |
  | 業界誌掲載/コンビニコンテスト | 人口1万人以上 かつ マップ内に5店舗以上（コンテスト賞金=店舗数×1000万円） |
  | アイドル | 来店客数1万人ごとに繰り返し発生（全店の人気度が100になる） |
- **ゲームオーバー条件**: 破産、またはクリア条件未達成のまま100年経過。

## 8. P1データ、2026-09-16の追加パスで転記完了

項目8で「存在を確認したが未転記」としていた5件は、ユーザーからの明示的な指示を受けて
同日中に転記した。詳細は `docs/decisions/0078-strategy-guide-deferred-p1-datasets.md` を参照。

- **店員候補ロースター**(35名、`STAFF_CANDIDATES`): 書籍頁127-133から全件転記。
  丸山昭夫のセキュリティ初期値のみ判読に自信が持てず`None`のまま。
- **顧客アーキタイプの時間帯別スケジュール**(21層・140行、`CUSTOMER_ARCHETYPES`+
  `CUSTOMER_VISIT_SCHEDULE`、新設の`CustomerVisitProfile`データクラス): 書籍頁134-143から
  全件転記。ただし9列の行動チューニング値(ス/素/マ/集/買/価/距/サ/平/休)は、解像度の制約で
  行ごとの桁数が必ずしも綺麗な9個にならず、個別命名フィールドへの割当は精度過信になるため
  `behavior_stats_raw`という生タプル1本にまとめた(時刻・所要時間・来店方法・所持金・
  欲しい商品は高信頼度で個別フィールド化)。
- **建物×客の欲しい品物データ**(59件、`TownBuildingProfile`+`TOWN_BUILDINGS`):
  書籍頁92-95の「名称/縦横/建物属性/建物価格/主なほしい品物」表から全件転記。時間帯の境界
  (朝7-11時/昼12-15時/夕16-19時/夜20-23時/深夜24-3時/早朝4-6時)も`TIME_OF_DAY_BUCKETS_HOURS`
  として記録。ページ右側の「主なほしい品物(深夜から早朝)」表は建物名ではなく商品組み合わせを
  キーにした別の頻度表であることが判明し、建物に紐付けて転記するのは誤った対応付けになる
  ため見送った。
- **設備×商品カテゴリ対応**(`FixtureDefinition.compatible_product_categories`):
  ○×マトリクス(書籍頁86-89、30行×26列の小さな記号表で誤読リスクが高い)ではなく、各設備
  データページ自身の「取扱商品」列(書籍頁110-119、大きな文字のテキストリスト)から転記。
  同じ情報をより信頼できる形式で取得できた。取扱商品が「なし」のレジ(register_1/register_3)
  と休憩室は`None`のまま。
- **商品カテゴリの季節フラグ**(`ProductCategoryPricing.seasonal_demand`): 冷たい飲料/
  アイスクリーム=夏季、温かい飲料/おでん/中華まん=冬期の5カテゴリのみ追加。同じ表の
  「1回補給」列と、もう1つの見出し不明列は、列の意味を確信できなかったため転記していない
  (サイドバー説明文からは「需要変動事例」を示す派生値の可能性があり、固定定数として
  EXPLICIT_NUMERIC扱いするには追加の一次資料確認が必要と判断)。

## 9. 2026-09-17 追記: ユーザー再アップロードによる高解像度スキャンでの解決

同日、ユーザーから同じ4分割PDFが再アップロードされ、前回より遥かに高い解像度でレンダリング
できることを確認した(`pdftoppm -r 400`)。これにより、上記の複数の「要判断・未解決」項目が
解決した。今回は**ユーザーから明示的に「衝突する内容は攻略本準拠」の指示**があったため、
従来の「既存値を上書きしない」方針を一時的に外し、以下を確定させた。

- **ベンチ維持費**: 160円/日(攻略本値)へ更新。既存168円(WIKI)を上書き。
- **ベンチ/噴水のservice_bonus**: ベンチ+4、噴水+30(攻略本値)へ更新。既存+3/+25(WIKI)を
  上書き。
- **`editable_floor`(店舗内寸法)**: 攻略本の店舗データ表(本1書籍頁106-109)から6バリエーション
  全ての店舗内寸法が判明。店舗1=5×8, 店舗2=8×5, 店舗3=7×10, 店舗4=10×7, 店舗5=8×12,
  店舗6=12×8。`small_top`=(5,8), `small_bottom`=(8,5), `medium_top`=(7,10),
  `medium_bottom`=(10,7), `large_top`=(8,12), `large_bottom`=(12,8) として反映(既存の
  (8,13)/(13,14)は上書き)。ただし**攻略本の店舗1/2/3/4/5/6という区別と、既存コードの
  top/bottomという区別の対応関係は攻略本からは確認できない**。リスト順(1,3,5→top;
  2,4,6→bottom)で対応付けたが、これは推測であり確認済みではない。`reference_sim`の
  `StoreGrid.from_store_variant`はこの値を直接グリッドサイズに使うため、`small_top`の
  内部グリッドが8×13(104マス)から5×8(40マス)へ縮小する、という実際のシミュレーション
  挙動への影響がある(`test_store_grid.py`で追認済み)。
- **大型店舗建設費の内部矛盾**: 既存の多数決(18,000,000円、攻略本内7箇所)は変更なし。
  これは「攻略本 vs 外部資料」の対立ではなく攻略本自身の内部矛盾のため、今回の
  「攻略本準拠」指示の対象外と判断した。
- **商品データ表(DATA LIST、本2系書籍頁84-85)の列を全て解決**: 前回「1回補給」列だと
  誤認していた列は、実際には**「1個の利益」列**(定価×利益率/100の検算値)だった。
  「1回補給」という列自体が原本に存在しない。`ProductCategoryPricing.restock_quantity`は
  `profit_per_unit_yen`へ改名(値は変更なし、全26カテゴリで定価×利益率/100と一致することを
  検算済み)。さらに未転記だった残り2列(前回「見出し不明」としていた列)は**「最大維持費」
  「最大収容力」**であると判明し、`max_maintenance_yen_per_day`/`max_capacity`として新規
  追加。表の最終列「需要数例」も新規追加(`demand_example_count`)したが、**攻略本自身が
  「いろいろな条件が重なるため、実際にはこうなるわけではない。あくまでも一例」と明記して
  おり、需要関数の係数として使ってはならない**(現金のみ原本表記が「?」のためNone)。
- **顧客データ表(本1書籍頁134-143、21アーキタイプ・143行)の10列行動データを完全に再分解**:
  前回「スキャン解像度の限界で桁数が9個/11個とブレる」としていた`behavior_stats_raw`が、
  今回のスキャンでは全行で明瞭に10桁(ス/素/マ/集/買/価/距/サ/平/休)判読できた。全143行を
  再転記し、`CUSTOMER_VISIT_SCHEDULE`を置き換えた(旧データは一部の桁が欠落した7〜9要素の
  タプルを含んでいた)。1件のみ原本の印字自体に異常値がある(男子小学生15:00来店行の「平」
  列が700 — 他行は全て100以下)。これは転記ミスではなく原本の誤植の可能性が高いと判断し、
  値を「正しく」補正せずそのまま保持した。

## 11. 2026-09-17 追記(その2): Codex側working memoとのクロスチェックで発見した新事実

ユーザーがCodex(ChatGPT)側で作成中の`MASTER_WORKING.md`(攻略本全ページのworking memo)を提示。
記載内容を既存コード・研究ドキュメントと照合し、原本PDFを直接再レンダリング(pdftoppm 400dpi)して
肉眼で裏取りした結果、以下4件が**既存コードに未反映の新事実**と判明したため実装した。
`strategy-guide-full-decode-2026-09-16.md`セクション41「推測禁止」リストの一部を解決する内容を含む。

- **販売許可の距離制限**(本1、書籍頁7): 店を中心とした同心円の図に「店建設可能=5」「たばこ販売可能=7」
  「酒類販売可能=11」「薬類販売可能=15」(マス)と明記。`PermitDefinition.exclusion_distance_tiles`は
  従来tobacco/alcohol/medicineの3件とも`None`だったが、tobacco=7/alcohol=11/medicine=15として反映
  (5は店舗同士の最低距離であり許可距離ではないため対象外)。単位がマス相当かどうか、
  Chebyshev距離かEuclidean距離かは攻略本からは確定しない。
- **販売許可の取得費用**(本1、書籍頁9): 「販売許可に必要な金額」表に酒類300万円/たばこ類700万円/
  薬類1000万円と明記(合計2000万円という本文とも整合)。`PermitDefinition.fee_yen`も従来3件とも
  `None`だったが反映。
- **来店手段別の商圏半径**(本1、書籍頁31): 「来店手段/エリア半径」表に徒歩=20/自転車=40/バイク=60/
  自動車=70と明記。これは`strategy-guide-full-decode-2026-09-16.md`が明示的に「商圏半径の内部計算式=
  推測禁止」としていた項目そのものであり、既存コードのどこにも記録されていなかった。新規
  `TradeAreaRadiusEntry`データクラス+`TRADE_AREA_RADIUS_TILES`テーブルとして追加。単位(マス相当か)、
  および複数店舗の商圏が重なった場合の顧客配分ルールは依然不明。
- **社員の複数スキル同時成長**(本1、書籍頁26): 「仕事内容とパラメータ変化の関係」図に、
  レジ1回→レジ+サービス、棚に補充1回→補充+清掃+警備、1エリア清掃→清掃+警備、とスキルの
  組が明記。既存の`staff.py`の`WORK_GROWTH_SKILL`はタスク:スキルが1:1(レジ→レジのみ、補充→補充のみ、
  清掃→清掃のみ)というモデルだったが、これをタスク:スキル1:多へ拡張した。ただし増分量が
  分かっているのは従来通り「補充→補充+1」「清掃→清掃+1」の2ペアのみで、新規追加された
  スキル(補充→清掃/警備、清掃→警備、レジ→サービス)の増分量は依然不明(成長機会の存在は
  確定、量は未確定という従来のレジ→レジ自体と同じ扱い)。`staff_growth_resolution.py`の
  `EVIDENCE_BACKED_UNIT_GROWTH`をタスク単独キーから(タスク,スキル)ペアキーへ変更し、
  未確定ペアが誤って+1を継承しないようにした。

なお、Codex側メモの他の項目(交番/消防署の効果範囲「半径7エリア」、社員上限3人、店舗評価の
増減式等)は既存コード・研究ドキュメントと照合済みで、新事実ではなく整合していることを確認した。
